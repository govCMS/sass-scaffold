docker-compose-yaml: docker-compose.yml

tasks:
  post-rollout:
    - run:
        name: Deploy GovCMS
        command: govcms-deploy
        service: cli

environments:
  master:
    cronjobs:
      - name: drush cron
        schedule: "M * * * *"
        command: 'drush cron --root=/app'
        service: cli
